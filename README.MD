```
#Lightbox-Gallery
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/steeven-js/Lightbox-Gallery.git
git push -u origin main
```
```
git add .
git commit -m "2e commit"
git push -u origin main
```
